PORT=3001
CORS_ORIGIN=http://localhost:5173

# Required: Get from https://console.anthropic.com
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Push notifications (generate with: node -e "const wp = require('web-push'); const keys = wp.generateVAPIDKeys(); console.log(keys)")
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
